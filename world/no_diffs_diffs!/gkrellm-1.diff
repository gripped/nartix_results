--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -22,7 +22,6 @@
   cd "$srcdir/$pkgname-$pkgver"
 
   make DESTDIR="$pkgdir" \
-    SERVICE_DIR="$pkgdir"/usr/lib/systemd/system \
     CFGDIR="$pkgdir"/etc \
     PREFIX=/usr install
 }
