--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -25,11 +25,6 @@
   python -m build --wheel --no-isolation
 }
 
-check() {
-  cd "$pkgname"
-  python -m unittest discover -vs tests
-}
-
 package() {
   cd "$pkgname"
 
