--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -26,7 +26,7 @@
 }
 
 build() {
-    arch-meson "$_pkgname-v$_tarpkgver" build \
+    artix-meson "$_pkgname-v$_tarpkgver" build \
         -Dmangoapp=true \
         -Dmangohudctl=true \
         -Dmangoapp_layer=true


############# Stripped diff below #################

--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -12,7 +12,7 @@
     sed -i "s/cmocka_dep = cmocka.get_variable('cmocka_dep')/cmocka_dep = dependency('cmocka')/g" meson.build
 }
 build() {
-    arch-meson "$_pkgname-v$_tarpkgver" build \
+    artix-meson "$_pkgname-v$_tarpkgver" build \
         -Dmangoapp=true \
         -Dmangohudctl=true \
         -Dmangoapp_layer=true
