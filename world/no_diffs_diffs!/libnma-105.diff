--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -48,7 +48,7 @@
     -D libnma_gtk4=true
   )
 
-  arch-meson libnma build "${meson_options[@]}"
+  artix-meson libnma build "${meson_options[@]}"
   meson compile -C build
 }
 


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -5,6 +5,6 @@
 local meson_options=(
 -D libnma_gtk4=true
 )
-arch-meson libnma build "${meson_options[@]}"
+artix-meson libnma build "${meson_options[@]}"
 meson compile -C build
 }
