--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -44,7 +44,7 @@
     -D vapi=true
   )
 
-  arch-meson librest build "${meson_options[@]}"
+  artix-meson librest build "${meson_options[@]}"
   meson compile -C build
 }
 


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -5,6 +5,6 @@
 local meson_options=(
 -D vapi=true
 )
-arch-meson librest build "${meson_options[@]}"
+artix-meson librest build "${meson_options[@]}"
 meson compile -C build
 }
