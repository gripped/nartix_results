--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -33,7 +33,7 @@
   local meson_options=(
   )
 
-  arch-meson zenity build "${meson_options[@]}"
+  artix-meson zenity build "${meson_options[@]}"
   meson compile -C build
 }
 


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -4,6 +4,6 @@
 build() {
 local meson_options=(
 )
-arch-meson zenity build "${meson_options[@]}"
+artix-meson zenity build "${meson_options[@]}"
 meson compile -C build
 }
