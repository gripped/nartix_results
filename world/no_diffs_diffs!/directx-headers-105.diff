--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -13,7 +13,7 @@
 build() {
   export CXXFLAGS="$CXXFLAGS -fno-lto"
 
-  arch-meson DirectX-Headers-$pkgver build \
+  artix-meson DirectX-Headers-$pkgver build \
     -Dbuild-test=false
   ninja -C build
 }


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -1,6 +1,6 @@
 build() {
 export CXXFLAGS="$CXXFLAGS -fno-lto"
-arch-meson DirectX-Headers-$pkgver build \
+artix-meson DirectX-Headers-$pkgver build \
 -Dbuild-test=false
 ninja -C build
 }
