--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -1,11 +1,11 @@
 
 pkgname=rtl-sdr
 pkgver=0.8.0
-pkgrel=5
+pkgrel=4
 epoch=1
 pkgdesc="Driver for Realtek RTL2832U, allowing general purpose software defined radio (SDR)."
 arch=('x86_64')
-url="https://github.com/librtlsdr/librtlsdr"
+url="https://sdr.osmocom.org/trac/wiki/rtl-sdr"
 license=('GPL')
 depends=('libusb>=1.0')
 makedepends=('git' 'cmake')
@@ -36,8 +36,5 @@
   make DESTDIR="$pkgdir" install
   install -Dm644 "$srcdir/librtlsdr-$pkgver/rtl-sdr.rules" "$pkgdir/usr/lib/udev/rules.d/10-rtl-sdr.rules"
   install -Dm644 "$srcdir/rtlsdr.conf" "$pkgdir/usr/lib/modprobe.d/rtlsdr.conf"
-
-  install -dm755 "${pkgdir}/usr/share/man/man1"
-  install -m644 "${srcdir}/librtlsdr-${pkgver}/debian/"{rtl_adsb,rtl_eeprom,rtl_fm,rtl_power,rtl_sdr,rtl_tcp,rtl_test}".1" "${pkgdir}/usr/share/man/man1/"
 }
 
