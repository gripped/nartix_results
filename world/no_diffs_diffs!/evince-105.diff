--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -69,7 +69,7 @@
     -D ps=enabled
   )
 
-  arch-meson evince build "${meson_options[@]}"
+  artix-meson evince build "${meson_options[@]}"
   meson compile -C build
 }
 


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -6,6 +6,6 @@
 local meson_options=(
 -D ps=enabled
 )
-arch-meson evince build "${meson_options[@]}"
+artix-meson evince build "${meson_options[@]}"
 meson compile -C build
 }
