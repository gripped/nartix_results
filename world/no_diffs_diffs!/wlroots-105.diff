--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -27,7 +27,6 @@
     'glslang'
     'meson'
     'ninja'
-    'systemd'
     'vulkan-headers'
     'wayland-protocols'
     'xorg-xwayland'
@@ -54,7 +53,7 @@
 )
 
 build() {
-    arch-meson "$pkgname-$pkgver" build
+    artix-meson "$pkgname-$pkgver" build
     ninja -C build
 }
 


############# Stripped diff below #################

--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -23,6 +23,6 @@
     'debug'
 )
 build() {
-    arch-meson "$pkgname-$pkgver" build
+    artix-meson "$pkgname-$pkgver" build
     ninja -C build
 }
