--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -73,7 +73,7 @@
     -D profile=release
   )
 
-  arch-meson geary build "${meson_options[@]}"
+  artix-meson geary build "${meson_options[@]}"
   meson compile -C build
 }
 


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -6,6 +6,6 @@
 local meson_options=(
 -D profile=release
 )
-arch-meson geary build "${meson_options[@]}"
+artix-meson geary build "${meson_options[@]}"
 meson compile -C build
 }
