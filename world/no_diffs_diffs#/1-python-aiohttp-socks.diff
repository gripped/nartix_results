--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -35,10 +35,6 @@
   python -m build --wheel --no-isolation
 }
 
-check() {
-  cd "${pkgname//python-/}"
-  pytest
-}
 
 package() {
   cd "${pkgname//python-/}"
