--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -54,7 +54,7 @@
   local meson_options=(
   )
 
-  arch-meson libnotify build "${meson_options[@]}"
+  artix-meson libnotify build "${meson_options[@]}"
   meson compile -C build
 }
 


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -9,6 +9,6 @@
 build() {
 local meson_options=(
 )
-arch-meson libnotify build "${meson_options[@]}"
+artix-meson libnotify build "${meson_options[@]}"
 meson compile -C build
 }
