--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -39,7 +39,7 @@
     -D sysprof=true
   )
 
-  arch-meson libdex build "${meson_options[@]}"
+  artix-meson libdex build "${meson_options[@]}"
   meson compile -C build
 }
 


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -6,6 +6,6 @@
 -D docs=true
 -D sysprof=true
 )
-arch-meson libdex build "${meson_options[@]}"
+artix-meson libdex build "${meson_options[@]}"
 meson compile -C build
 }
