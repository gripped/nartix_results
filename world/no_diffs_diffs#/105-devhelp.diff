--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -26,7 +26,7 @@
     -D plugin_vim=true
   )
 
-  arch-meson devhelp build "${meson_options[@]}"
+  artix-meson devhelp build "${meson_options[@]}"
   meson compile -C build
 }
 


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -8,6 +8,6 @@
 -D plugin_gedit=true
 -D plugin_vim=true
 )
-arch-meson devhelp build "${meson_options[@]}"
+artix-meson devhelp build "${meson_options[@]}"
 meson compile -C build
 }
