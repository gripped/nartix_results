--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -50,7 +50,7 @@
     -D sysprof=enabled
   )
 
-  arch-meson pango build "${meson_options[@]}"
+  artix-meson pango build "${meson_options[@]}"
   meson compile -C build
 }
 


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -8,6 +8,6 @@
 -D man-pages=true
 -D sysprof=enabled
 )
-arch-meson pango build "${meson_options[@]}"
+artix-meson pango build "${meson_options[@]}"
 meson compile -C build
 }
