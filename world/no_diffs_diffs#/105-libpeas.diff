--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -45,7 +45,7 @@
     -D vapi=true
   )
 
-  arch-meson libpeas build "${meson_options[@]}"
+  artix-meson libpeas build "${meson_options[@]}"
   meson compile -C build
 }
 


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -7,6 +7,6 @@
 -D python3=false
 -D vapi=true
 )
-arch-meson libpeas build "${meson_options[@]}"
+artix-meson libpeas build "${meson_options[@]}"
 meson compile -C build
 }
