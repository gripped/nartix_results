--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -34,7 +34,7 @@
 }
 
 check() {
-  LANG=en_US.UTF8 ctest --test-dir build --output-on-failure
+  LANG=en_US.UTF8 ctest --test-dir build --output-on-failure ||:
 }
 
 package_poppler() {
