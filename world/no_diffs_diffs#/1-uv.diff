--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -2,24 +2,18 @@
 pkgbase=uv
 pkgname=("$pkgbase" "python-$pkgbase"{,-build})
 pkgver=0.9.9
-pkgrel=2
+pkgrel=1
 pkgdesc='An extremely fast Python package installer and resolver written in Rust'
 arch=('x86_64')
 url="https://github.com/astral-sh/uv"
 license=('MIT' 'Apache-2.0')
-depends=(
-  'bzip2'
-  'gcc-libs'
-  'glibc'
-)
-makedepends=(
-  'cargo'
-  'cmake'
-  'git'
-  'maturin'
-  'python-installer'
-  'xz'
-)
+depends=('gcc-libs' # 'libgcc_s.so'
+         'glibc' # 'libc.so' 'libm.so'
+         'zlib' # 'libz.so'
+         'bzip2' # 'libbz2.so'
+         )
+makedepends=('cargo' 'maturin' 'python-installer' 'cmake' 'git')
+checkdepends=('python' 'python-zstandard' 'libxcrypt-compat' 'clang')
 options=('!lto')
 source=("git+$url.git#tag=$pkgver")
 sha256sums=('4db3f50dd383a4c03edba98b4f954a7f45a4c563281807bf8fcf9a61876f407f')
@@ -56,12 +50,6 @@
 }
 
 package_uv() {
-  depends=(
-    gcc-libs
-    glibc
-    bzip2
-  )
-
   cd "$pkgbase"
   _package_common
   local _target="target/$(rustc -vV | sed -n 's/host: //p')/release/uv"
@@ -74,11 +62,7 @@
 }
 
 package_python-uv() {
-  pkgdesc+=' - Python wrapper'
-  depends=(
-    python
-  )
-
+  desc+=' (python wrapper)'
   cd "$pkgbase"
   _package_common
   depends=(python "$pkgbase=$pkgver")
@@ -87,16 +71,7 @@
 }
 
 package_python-uv-build() {
-  pkgdesc+=' - Python build backend'
-  depends=(
-    bzip2
-    gcc-libs
-    glibc
-    python
-    xz
-  )
-  groups=(python-build-backend)
-
+  desc+=' (python build backend)'
   cd "$pkgbase"
   _package_common
   python -m installer -d "$pkgdir" target/wheels/uv_build-$pkgver-*.whl


############# Stripped diff below #################

--- arch_depends
+++ artix_depends
@@ -8,4 +8,4 @@
 python
 python-installer
 uv
-xz
+zlib