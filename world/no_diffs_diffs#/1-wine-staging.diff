--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -24,6 +24,7 @@
 arch=(x86_64)
 options=(!lto pestrip)
 license=(LGPL-2.1-or-later)
+CFLAGS+=" -Wno-error=incompatible-pointer-types -fPIC"
 depends=(
   attr
   desktop-file-utils
@@ -40,6 +41,7 @@
   libxrandr
   wayland
 )
+CFLAGS+=" -Wno-error=incompatible-pointer-types -fPIC"
 makedepends=(
   alsa-lib
   ffmpeg
@@ -68,6 +70,7 @@
   vulkan-headers
   vulkan-icd-loader
 )
+CFLAGS+=" -Wno-error=incompatible-pointer-types -fPIC"
 optdepends=(
   alsa-lib
   alsa-plugins
