--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -54,7 +54,7 @@
     -D tests=true
   )
 
-  arch-meson cheese build "${meson_options[@]}"
+  artix-meson cheese build "${meson_options[@]}"
   meson compile -C build
 }
 


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -8,6 +8,6 @@
 local meson_options=(
 -D tests=true
 )
-arch-meson cheese build "${meson_options[@]}"
+artix-meson cheese build "${meson_options[@]}"
 meson compile -C build
 }
