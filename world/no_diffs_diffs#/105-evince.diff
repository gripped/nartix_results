--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -56,7 +56,7 @@
     -D ps=enabled
   )
 
-  arch-meson evince build "${meson_options[@]}"
+  artix-meson evince build "${meson_options[@]}"
   meson compile -C build
 }
 


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -5,6 +5,6 @@
 local meson_options=(
 -D ps=enabled
 )
-arch-meson evince build "${meson_options[@]}"
+artix-meson evince build "${meson_options[@]}"
 meson compile -C build
 }
