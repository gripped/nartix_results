--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -35,7 +35,7 @@
 }
 
 build() {
-  arch-meson paprefs build -D lynx=true
+  artix-meson paprefs build -D lynx=true
   meson compile -C build
 }
 


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -2,6 +2,6 @@
 cd paprefs
 }
 build() {
-arch-meson paprefs build -D lynx=true
+artix-meson paprefs build -D lynx=true
 meson compile -C build
 }
