--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -22,7 +22,7 @@
              networkmanager-qt)
 optdepends=('libnl: network usage monitor'
             'networkmanager-qt: network usage monitor'
-            'systemd-libs: GPU monitor')
+            'libudev: GPU monitor')
 groups=(plasma)
 source=(https://download.kde.org/stable/plasma/$_dirver/$pkgname-$pkgver.tar.xz{,.sig})
 sha256sums=('6b5e2f2a6aaae97489e75818e7f83c2586578022760821c677cdebcefa542e36'
@@ -42,4 +42,6 @@
 package() {
   DESTDIR="$pkgdir" cmake --install build
   setcap CAP_PERFMON=+ep "$pkgdir"/usr/lib/ksystemstats_intel_helper
+
+  rm -r $pkgdir/usr/lib/systemd
 }
