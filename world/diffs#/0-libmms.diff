--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -20,6 +20,8 @@
   cd "$pkgname-$pkgver"
 
   patch -p1 -i "$srcdir/libmms-Remove-requires-glib2.0.patch"
+
+  autoreconf -vfi
 }
 
 build() {


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -1,6 +1,7 @@
 prepare() {
 cd "$pkgname-$pkgver"
 patch -p1 -i "$srcdir/libmms-Remove-requires-glib2.0.patch"
+autoreconf -vfi
 }
 build() {
 cd "$pkgname-$pkgver"
