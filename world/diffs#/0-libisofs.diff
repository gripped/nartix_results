--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -14,7 +14,6 @@
 
 build() {
   cd "${pkgname}"-${pkgver}
-  autoreconf -fvi
   ./configure --prefix=/usr \
 	--enable-libacl --enable-xattr --disable-static
   make


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -1,6 +1,5 @@
 build() {
 cd "${pkgname}"-${pkgver}
-autoreconf -fvi
 ./configure --prefix=/usr \
 --enable-libacl --enable-xattr --disable-static
 make
