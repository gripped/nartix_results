--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -1,19 +1,22 @@
 
 pkgname=openmp
-pkgver=22.1.1
+pkgver=21.1.8
 pkgrel=1
 pkgdesc="LLVM OpenMP Runtime Library"
 arch=('x86_64')
 url="https://openmp.llvm.org/"
 license=('Apache-2.0 WITH LLVM-exception')
-depends=('llvm-libs' 'libelf' 'libffi' 'glibc' 'libstdc++' 'libgcc')
+depends=('llvm-libs' 'libelf' 'libffi')
 makedepends=('llvm' 'clang' 'cmake' 'ninja' 'python')
 optdepends=('cuda: offloading to NVIDIA GPUs'
             'hsa-rocr: offloading to AMD GPUs')
 options=('!lto') # https://bugzilla.redhat.com/show_bug.cgi?id=1988155
 _source_base=https://github.com/llvm/llvm-project/releases/download/llvmorg-$pkgver
-source=($_source_base/llvm-project-$pkgver.src.tar.xz{,.sig})
-sha256sums=('9c6f37f6f5f68d38f435d25f770fc48c62d92b2412205767a16dac2c942f0c95'
+source=($_source_base/openmp-$pkgver.src.tar.xz{,.sig}
+        $_source_base/cmake-$pkgver.src.tar.xz{,.sig})
+sha256sums=('856b023748b41ac7b2c83fd8e9f765ff48a4df2fe6777d2811ef7c7ed8f2f977'
+            'SKIP'
+            '85735f20fd8c81ecb0a09abb0c267018475420e93b65050cc5b7634eab744de9'
             'SKIP')
 validpgpkeys=('474E22316ABF4785A88C6E8EA2C794A986419D8A'  # Tom Stellard <tstellar@redhat.com>
               'D574BD5D1D0E98895E3BF90044F2485E45D59042'  # Tobias Hieta <tobias@hieta.se>
@@ -22,12 +25,13 @@
 )
 
 prepare() {
-  cd llvm-project-$pkgver.src/openmp
+  mv cmake{-$pkgver.src,}
+  cd openmp-$pkgver.src
   mkdir build
 }
 
 build() {
-  cd llvm-project-$pkgver.src/openmp/build
+  cd openmp-$pkgver.src/build
 
   local cmake_args=(
     -G Ninja
@@ -40,10 +44,10 @@
 }
 
 package() {
-  cd llvm-project-$pkgver.src/openmp/build
+  cd openmp-$pkgver.src/build
 
   DESTDIR="$pkgdir" ninja install
-  install -Dm644 "$srcdir/llvm-project-$pkgver.src/openmp/LICENSE.TXT" "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
+  install -Dm644 ../LICENSE.TXT "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
   rm "$pkgdir/usr/lib/libarcher_static.a"
 
   python -m compileall -d /usr/share "$pkgdir/usr/share"


############# Stripped diff below #################

--- arch_depends
+++ artix_depends
@@ -1,10 +1,7 @@
 clang
 cmake
-glibc
 libelf
 libffi
-libgcc
-libstdc++
 llvm
 llvm-libs
 ninja

--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -1,9 +1,10 @@
 prepare() {
-cd llvm-project-$pkgver.src/openmp
+mv cmake{-$pkgver.src,}
+cd openmp-$pkgver.src
 mkdir build
 }
 build() {
-cd llvm-project-$pkgver.src/openmp/build
+cd openmp-$pkgver.src/build
 local cmake_args=(
 -G Ninja
 -DCMAKE_BUILD_TYPE=Release
