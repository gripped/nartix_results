--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -16,7 +16,6 @@
 prepare() {
   cd ${pkgname}-${pkgver}
   patch -Np1 -i ../libpagemaker-0.0.4-const-ref-exception.patch
-  autoreconf -vfi
 }
 
 build() {


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -1,7 +1,6 @@
 prepare() {
 cd ${pkgname}-${pkgver}
 patch -Np1 -i ../libpagemaker-0.0.4-const-ref-exception.patch
-autoreconf -vfi
 }
 build() {
 cd ${pkgname}-${pkgver}
