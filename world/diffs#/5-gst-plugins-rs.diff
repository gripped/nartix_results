--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -3,23 +3,28 @@
 pkgname=(
   gst-webrtc-signalling-server
   gst-plugin-aws
+  gst-plugin-burn
   gst-plugin-cdg
   gst-plugin-claxon
+  gst-plugin-csound
   gst-plugin-dav1d
+  gst-plugin-debugseimetainserter
+  gst-plugin-deepgram
+  gst-plugin-demucs
   gst-plugin-elevenlabs
   gst-plugin-fallbackswitch
   gst-plugin-ffv1
-  gst-plugin-fmp4
   gst-plugin-gif
   gst-plugin-gopbuffer
   gst-plugin-gtk4
   gst-plugin-hlsmultivariantsink
   gst-plugin-hlssink3
   gst-plugin-hsv
+  gst-plugin-icecast
+  gst-plugin-isobmff
   gst-plugin-json
   gst-plugin-lewton
   gst-plugin-livesync
-  gst-plugin-mp4
   gst-plugin-mpegtslive
   gst-plugin-originalbuffer
   gst-plugin-quinn
@@ -27,7 +32,9 @@
   gst-plugin-rav1e
   gst-plugin-regex
   gst-plugin-reqwest
+  gst-plugin-rsanalytics
   gst-plugin-rsaudiofx
+  gst-plugin-rsaudioparsers
   gst-plugin-rsclosedcaption
   gst-plugin-rsfile
   gst-plugin-rsflv
@@ -45,15 +52,17 @@
   gst-plugin-speechmatics
   gst-plugin-spotify
   gst-plugin-streamgrouper
+  gst-plugin-textaccumulate
   gst-plugin-textahead
   gst-plugin-textwrap
   gst-plugin-threadshare
   gst-plugin-togglerecord
   gst-plugin-uriplaylistbin
   gst-plugin-webrtchttp
+  gst-plugin-whisper
 )
-pkgver=0.14.4
-pkgrel=2
+pkgver=0.15.0
+pkgrel=1
 pkgdesc="Multimedia graph framework"
 url="https://gstreamer.freedesktop.org/"
 arch=(x86_64)
@@ -67,6 +76,9 @@
 makedepends=(
   cairo
   cargo-c
+  clang
+  cmake
+  csound
   dav1d
   fontconfig
   freetype2
@@ -84,12 +96,14 @@
   openssl
   pango
   rust
+  vulkan-headers
+  vulkan-icd-loader
 )
 options=(!lto)
 source=(
   "git+https://gitlab.freedesktop.org/gstreamer/gst-plugins-rs.git?signed#tag=$pkgver"
 )
-b2sums=('50e6cb80b107fbaafc947f8c29c4167ed622ad77fa9da3bb12864c85d3c2b15f1d9914a4654bdf5f2f095238be30ff7c392dc0b21b62ca13fbd3343ce7f29d5f')
+b2sums=('0b62fbbad299951d187eb69b2a0a66ce1496f86b4d0aa85378a4a7ba422ec41bf8b60d230fec5f91d5b0a1550f30ddc4f07a39c345fbde6e4c11e951223f5d12')
 validpgpkeys=(
   7F4BC7CC3CA06F97336BBFEB0668CC1486C2D7B5 # Sebastian Dröge <sebastian@centricular.com>
 )
@@ -97,11 +111,10 @@
 _cargo_c_options=(
   --prefix /usr
   --library-type cdylib
-  --features asm,dmabuf,dssim,gtk_v4_20,v1_22,v1_26,waylandegl,x11egl,x11glx
+  --features asm,dmabuf,dssim,gtk_v4_20,v1_22,v1_24,v1_26,v1_28,vulkan,waylandegl,x11egl,x11glx
   --workspace
-  --exclude gst-plugin-analytics
-  --exclude gst-plugin-csound
   --exclude gst-plugin-ndi
+  --exclude gst-plugin-validate
   --exclude gst-plugin-vvdec
 )
 
@@ -113,7 +126,7 @@
 
 prepare() {
   cd $pkgbase
-  cargo fetch --locked --target "$(rustc --print host-tuple)"
+  cargo fetch --locked --target host-tuple
 }
 
 build() {
@@ -166,6 +179,17 @@
   mv plugin-aws/* "$pkgdir"
 }
 
+package_gst-plugin-burn() {
+  pkgdesc+=" - burn plugin"
+  depends+=(
+    gst-plugins-bad-libs
+    gst-plugins-base-libs
+    vulkan-icd-loader
+  )
+
+  mv plugin-burn/* "$pkgdir"
+}
+
 package_gst-plugin-cdg() {
   pkgdesc+=" - cdg plugin"
   license=("MIT OR Apache-2.0")
@@ -188,6 +212,16 @@
     -t "$pkgdir/usr/share/licenses/$pkgname"
 }
 
+package_gst-plugin-csound() {
+  pkgdesc+=" - csound plugin"
+  depends+=(
+    csound
+    gst-plugins-base-libs
+  )
+
+  mv plugin-csound/* "$pkgdir"
+}
+
 package_gst-plugin-dav1d() {
   pkgdesc+=" - dav1d plugin"
   license=("MIT OR Apache-2.0")
@@ -202,6 +236,30 @@
     -t "$pkgdir/usr/share/licenses/$pkgname"
 }
 
+package_gst-plugin-debugseimetainserter() {
+  pkgdesc+=" - debugseimetainserter plugin"
+  depends+=(gst-plugins-base-libs)
+
+  mv plugin-debugseimetainserter/* "$pkgdir"
+}
+
+package_gst-plugin-deepgram() {
+  pkgdesc+=" - deepgram plugin"
+  depends+=(
+    gst-plugins-base-libs
+    openssl
+  )
+
+  mv plugin-deepgram/* "$pkgdir"
+}
+
+package_gst-plugin-demucs() {
+  pkgdesc+=" - demucs plugin"
+  depends+=(gst-plugins-base-libs)
+
+  mv plugin-demucs/* "$pkgdir"
+}
+
 package_gst-plugin-elevenlabs() {
   pkgdesc+=" - elevenlabs plugin"
   depends+=(gst-plugins-base-libs)
@@ -230,13 +288,6 @@
     -t "$pkgdir/usr/share/licenses/$pkgname"
 }
 
-package_gst-plugin-fmp4() {
-  pkgdesc+=" - fmp4 plugin"
-  depends+=(gst-plugins-base-libs)
-
-  mv plugin-fmp4/* "$pkgdir"
-}
-
 package_gst-plugin-gif() {
   pkgdesc+=" - gif plugin"
   license=("MIT OR Apache-2.0")
@@ -269,7 +320,7 @@
 package_gst-plugin-hlsmultivariantsink() {
   pkgdesc+=" - hlsmultivariantsink plugin"
   depends+=(
-    gst-plugin-hlssink3
+    "gst-plugin-hlssink3=$pkgver-$pkgrel"
     gst-plugins-base-libs
   )
 
@@ -279,7 +330,7 @@
 package_gst-plugin-hlssink3() {
   pkgdesc+=" - hlssink3 plugin"
   depends+=(
-    gst-plugin-fmp4
+    "gst-plugin-isobmff=$pkgver-$pkgrel"
     gst-plugins-base-libs
   )
 
@@ -297,6 +348,24 @@
     -t "$pkgdir/usr/share/licenses/$pkgname"
 }
 
+package_gst-plugin-icecast() {
+  pkgdesc+=" - icecast plugin"
+  depends+=(gst-plugins-base-libs)
+
+  mv plugin-icecast/* "$pkgdir"
+}
+
+package_gst-plugin-isobmff() {
+  pkgdesc+=" - isobmff plugin"
+  depends+=(gst-plugins-base-libs)
+  replaces=(
+    'gst-plugin-fmp4<=0.14.4-2'
+    'gst-plugin-mp4<=0.14.4-2'
+  )
+
+  mv plugin-isobmff/* "$pkgdir"
+}
+
 package_gst-plugin-json() {
   pkgdesc+=" - json plugin"
 
@@ -321,13 +390,6 @@
   mv plugin-livesync/* "$pkgdir"
 }
 
-package_gst-plugin-mp4() {
-  pkgdesc+=" - mp4 plugin"
-  depends+=(gst-plugins-base-libs)
-
-  mv plugin-mp4/* "$pkgdir"
-}
-
 package_gst-plugin-mpegtslive() {
   pkgdesc+=" - mpegtslive plugin"
 
@@ -381,6 +443,16 @@
     -t "$pkgdir/usr/share/licenses/$pkgname"
 }
 
+package_gst-plugin-rsanalytics() {
+  pkgdesc+=" - rsanalytics plugin"
+  depends+=(
+    gst-plugins-bad-libs
+    gst-plugins-base-libs
+  )
+
+  mv plugin-rsanalytics/* "$pkgdir"
+}
+
 package_gst-plugin-rsaudiofx() {
   pkgdesc+=" - rsaudiofx plugin"
   depends+=(gst-plugins-base-libs)
@@ -388,9 +460,17 @@
   mv plugin-rsaudiofx/* "$pkgdir"
 }
 
+package_gst-plugin-rsaudioparsers() {
+  pkgdesc+=" - rsaudioparsers plugin"
+  depends+=(gst-plugins-base-libs)
+
+  mv plugin-rsaudioparsers/* "$pkgdir"
+}
+
 package_gst-plugin-rsclosedcaption() {
   pkgdesc+=" - rsclosedcaption plugin"
   depends+=(
+    "gst-plugin-textaccumulate=$pkgver-$pkgrel"
     "gst-plugin-textwrap=$pkgver-$pkgrel"
     cairo
     gst-plugins-bad
@@ -551,6 +631,12 @@
   mv plugin-streamgrouper/* "$pkgdir"
 }
 
+package_gst-plugin-textaccumulate() {
+  pkgdesc+=" - textaccumulate plugin"
+
+  mv plugin-textaccumulate/* "$pkgdir"
+}
+
 package_gst-plugin-textahead() {
   pkgdesc+=" - textahead plugin"
 
@@ -595,3 +681,14 @@
   mv plugin-webrtchttp/* "$pkgdir"
 }
 
+package_gst-plugin-whisper() {
+  pkgdesc+=" - whisper plugin"
+  depends+=(
+    gst-plugins-base-libs
+    libstdc++
+    vulkan-icd-loader
+  )
+
+  mv plugin-whisper/* "$pkgdir"
+}
+


############# Stripped diff below #################

--- arch_depends
+++ artix_depends
@@ -1,5 +1,8 @@
 cairo
 cargo-c
+clang
+cmake
+csound
 dav1d
 fontconfig
 freetype2
@@ -7,9 +10,10 @@
 glib2
 glibc
 graphene
-gst-plugin-fmp4
 gst-plugin-hlssink3
+gst-plugin-isobmff
 gst-plugin-rsrtp
+gst-plugin-textaccumulate
 gst-plugin-textwrap
 gst-plugins-bad
 gst-plugins-bad-libs
@@ -26,3 +30,5 @@
 openssl
 pango
 rust
+vulkan-headers
+vulkan-icd-loader

--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -1,6 +1,6 @@
 prepare() {
 cd $pkgbase
-cargo fetch --locked --target "$(rustc --print host-tuple)"
+cargo fetch --locked --target host-tuple
 }
 build() {
 cd $pkgbase
