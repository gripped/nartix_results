--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -32,6 +32,7 @@
 
 
 build() {
+    env
 	cd "${pkgname}-${pkgver}"
 	./make.py build --dist unix
 	python -m build --wheel --no-isolation


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -1,4 +1,5 @@
 build() {
+env
 cd "${pkgname}-${pkgver}"
 ./make.py build --dist unix
 python -m build --wheel --no-isolation
