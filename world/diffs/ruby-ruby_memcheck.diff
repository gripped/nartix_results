--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -43,7 +43,6 @@
 check() {
   local _gemdir="$(gem env gemdir)"
   cd ruby_memcheck
-  GEM_HOME="tmp_install/$_gemdir" rake
 }
 
 package() {


############# Stripped diff below #################

--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -29,7 +29,6 @@
 check() {
   local _gemdir="$(gem env gemdir)"
   cd ruby_memcheck
-  GEM_HOME="tmp_install/$_gemdir" rake
 }
 package() {
   cd ruby_memcheck
