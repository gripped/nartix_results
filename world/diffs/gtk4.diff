--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -49,7 +49,6 @@
   libxrender
   pango
   shared-mime-info
-  tracker3
   wayland
 )
 makedepends=(
@@ -65,6 +64,7 @@
   wayland-protocols
 )
 checkdepends=(weston)
+options=(debug)
 _commit=55bc7808cc7796b06c27b1e64f608cd83b9b18ec  # tags/4.10.4^0
 source=(
   "git+https://gitlab.gnome.org/GNOME/gtk.git#commit=$_commit"
@@ -93,11 +93,10 @@
     -D colord=enabled
     -D gtk_doc=true
     -D man-pages=true
-    -D tracker=enabled
   )
 
   CFLAGS+=" -DG_DISABLE_CAST_CHECKS"
-  arch-meson gtk build "${meson_options[@]}"
+  artix-meson gtk build "${meson_options[@]}"
   meson compile -C build
 }
 


############# Stripped diff below #################

--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -41,7 +41,6 @@
   libxrender
   pango
   shared-mime-info
-  tracker3
   wayland
 )
 makedepends=(
@@ -56,6 +55,7 @@
   shaderc
   wayland-protocols
 )
 prepare() {
   cd gtk
 }
@@ -66,10 +66,9 @@
     -D colord=enabled
     -D gtk_doc=true
     -D man-pages=true
-    -D tracker=enabled
   )
   CFLAGS+=" -DG_DISABLE_CAST_CHECKS"
-  arch-meson gtk build "${meson_options[@]}"
+  artix-meson gtk build "${meson_options[@]}"
   meson compile -C build
 }
 package_gtk4() {
