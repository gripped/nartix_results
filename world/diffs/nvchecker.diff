--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -52,6 +52,8 @@
     git log --oneline -1 "${_c}"
     git cherry-pick -n "${_c}"
   done
+
+  sed -e 's|core|system|' -i tests/test_alpmfiles.py
 }
 
 build() {
@@ -63,7 +65,7 @@
 
 check() {
   cd nvchecker
-  pytest
+  pytest # || :
 }
 
 package() {


############# Stripped diff below #################

--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -14,6 +14,7 @@
     git log --oneline -1 "${_c}"
     git cherry-pick -n "${_c}"
   done
+  sed -e 's|core|system|' -i tests/test_alpmfiles.py
 }
 build() {
   cd nvchecker
