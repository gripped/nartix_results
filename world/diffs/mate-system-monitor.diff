--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -20,7 +20,7 @@
 	            --prefix=/usr \
 	           --libexecdir=/usr/lib/${pkgname} \
 	           --localstatedir=/var \
-	           --enable-systemd
+	           --disable-systemd
 	make
 }
 


############# Stripped diff below #################

--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -9,6 +9,6 @@
 	            --prefix=/usr \
 	           --libexecdir=/usr/lib/${pkgname} \
 	           --localstatedir=/var \
-	           --enable-systemd
+	           --disable-systemd
 	make
 }
