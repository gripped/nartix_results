--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -17,7 +17,6 @@
 
 build() {
   cd "${pkgname}"-${pkgver/.pl01/}
-  autoreconf -fvi
   ./configure --prefix=/usr
   make
 }


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -1,6 +1,5 @@
 build() {
 cd "${pkgname}"-${pkgver/.pl01/}
-autoreconf -fvi
 ./configure --prefix=/usr
 make
 }
