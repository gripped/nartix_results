--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -1,9 +1,9 @@
 
 pkgname=mpv
 epoch=1
-_tag='3996724d3fa1c51cc7998f3de2e22e2c99e6d270' # git rev-parse v${pkgver}
-pkgver=0.36.0
-pkgrel=1
+_tag='5ae02ccad10542a74b397e39800ec01def0db4e1' # git rev-parse v${pkgver}
+pkgver=0.35.1
+pkgrel=8.2
 pkgdesc='a free, open source, and cross-platform media player'
 arch=('x86_64')
 license=('GPL3')
@@ -28,6 +28,15 @@
 source=("git+https://github.com/mpv-player/mpv.git#tag=${_tag}?signed")
 sha256sums=('SKIP')
 
+prepare() {
+  cd "${pkgname}"
+
+  git cherry-pick -n \
+    985655ebfd77ceddc44d76f8cc6dc446002f34ee \
+    a5961ad096b1361a12f836c8b170fc748f46962a
+  git cherry-pick -n 3a2f8d42ab203c961e50ce198ca4eb232de3819a
+}
+
 build() {
   local _meson_options=(
     --auto-features auto
@@ -41,7 +50,7 @@
     -Dlibarchive=enabled
   )
 
-  arch-meson "${pkgname}" build "${_meson_options[@]}"
+  artix-meson "${pkgname}" build "${_meson_options[@]}"
 
   meson compile -C build
 }


############# Stripped diff below #################

--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -11,6 +11,13 @@
          'libshaderc_shared.so' 'uchardet' 'vulkan-icd-loader' 'wayland' 'zlib')
 provides=('libmpv.so')
 options=('!emptydirs')
+prepare() {
+  cd "${pkgname}"
+  git cherry-pick -n \
+    985655ebfd77ceddc44d76f8cc6dc446002f34ee \
+    a5961ad096b1361a12f836c8b170fc748f46962a
+  git cherry-pick -n 3a2f8d42ab203c961e50ce198ca4eb232de3819a
+}
 build() {
   local _meson_options=(
     --auto-features auto
@@ -21,6 +28,6 @@
     -Ddvdnav=enabled
     -Dlibarchive=enabled
   )
-  arch-meson "${pkgname}" build "${_meson_options[@]}"
+  artix-meson "${pkgname}" build "${_meson_options[@]}"
   meson compile -C build
 }
