--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -1,8 +1,8 @@
 
 pkgbase=podman
 pkgname=(podman podman-docker)
-pkgver=4.6.1
-_commit=f3069b3ff48e30373c33b3f5976f15abf8cfee20  #refs/tags/v4.6.1^{}
+pkgver=4.6.0
+_commit=38e6fab9664c6e59b66e73523b307a56130316ae  #refs/tags/v4.6.0^{}
 pkgrel=1
 pkgdesc='Tool and library for running OCI-based containers in pods'
 arch=(x86_64)
@@ -18,7 +18,7 @@
   go-md2man
   gpgme
   libseccomp
-  systemd
+  libudev
 )
 options=(!lto)
 source=(
@@ -43,7 +43,7 @@
 }
 
 build() {
-  export BUILDTAGS='apparmor seccomp systemd'
+  export BUILDTAGS='apparmor seccomp'
   export CGO_CPPFLAGS="${CPPFLAGS}"
   export CGO_CFLAGS="${CFLAGS}"
   export CGO_CXXFLAGS="${CXXFLAGS}"
@@ -92,4 +92,5 @@
   provides=(docker)
 
   make -j1 install.docker-full DESTDIR="$pkgdir" PREFIX=/usr -C $pkgbase
+  rm -rf "$pkgdir"/usr/lib/systemd
 }


############# Stripped diff below #################

--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -10,14 +10,14 @@
   go-md2man
   gpgme
   libseccomp
-  systemd
+  libudev
 )
 options=(!lto)
 prepare() {
   patch -Np1 -d $pkgname -i ../$pkgname-4.6.0-defaultinitpath.patch
 }
 build() {
-  export BUILDTAGS='apparmor seccomp systemd'
+  export BUILDTAGS='apparmor seccomp'
   export CGO_CPPFLAGS="${CPPFLAGS}"
   export CGO_CFLAGS="${CFLAGS}"
   export CGO_CXXFLAGS="${CXXFLAGS}"
@@ -51,4 +51,5 @@
   conflicts=(docker)
   provides=(docker)
   make -j1 install.docker-full DESTDIR="$pkgdir" PREFIX=/usr -C $pkgbase
+  rm -rf "$pkgdir"/usr/lib/systemd
 }
