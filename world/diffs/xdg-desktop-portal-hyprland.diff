--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -10,6 +10,7 @@
 provides=(xdg-desktop-portal-impl)
 depends=(libinih
          libuuid.so
+         libelogind
          pipewire
          qt6-base
          qt6-wayland
@@ -38,7 +39,7 @@
 
 build() {
     cd "$_archive"
-    arch-meson build
+    artix-meson -Dsystemd=disabled build
     ninja -C build
     make -C hyprland-share-picker all
 }


############# Stripped diff below #################

--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -3,6 +3,7 @@
 provides=(xdg-desktop-portal-impl)
 depends=(libinih
          libuuid.so
+         libelogind
          pipewire
          qt6-base
          qt6-wayland
@@ -16,7 +17,7 @@
 }
 build() {
     cd "$_archive"
-    arch-meson build
+    artix-meson -Dsystemd=disabled build
     ninja -C build
     make -C hyprland-share-picker all
 }
