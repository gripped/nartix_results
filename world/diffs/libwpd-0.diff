--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -19,7 +19,6 @@
 prepare() {
   cd ${pkgname}-${pkgver}
   patch -Np1 -i ../libwpd-gcc11.patch
-  autoreconf -vfi
 }
 
 build() {


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -1,7 +1,6 @@
 prepare() {
 cd ${pkgname}-${pkgver}
 patch -Np1 -i ../libwpd-gcc11.patch
-autoreconf -vfi
 }
 build() {
 cd ${pkgname}-${pkgver}
