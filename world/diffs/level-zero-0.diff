--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -1,54 +1,52 @@
 
 pkgbase=level-zero
-pkgname=(
-    'level-zero-headers'
-    'level-zero-loader')
-pkgver=1.25.2
+pkgname=(level-zero-headers level-zero-loader)
+pkgver=1.24.3
 pkgrel=1
-pkgdesc='API for accessing low level interfaces in oneAPI platform devices'
-arch=('x86_64')
+pkgdesc="API for accessing low level interfaces in oneAPI platform devices"
+arch=(x86_64)
 url='https://github.com/oneapi-src/level-zero/'
-license=('MIT')
+license=(MIT)
 makedepends=(
-    'cmake'
-    'git'
-    'opencl-headers'
-    'spdlog')
-source=("https://github.com/oneapi-src/level-zero/archive/v${pkgver}/${pkgbase}-${pkgver}.tar.gz")
-sha256sums=('c8f788e330e120e951bdcd7246d075244a6ea7aadd60510ecd414636d52ffb29')
+  cmake
+  git
+  opencl-headers
+  spdlog)
+source=(https://github.com/oneapi-src/level-zero/archive/v${pkgver}/${pkgbase}-${pkgver}.tar.gz)
+sha256sums=('c7c53f62bd91fb845c04cdbd05ed435550d37b6a8a068ef3319747df79c380c3')
 
 prepare() {
-    printf '%s\n' "${pkgver##*.}" > "${pkgbase}-${pkgver}/VERSION_PATCH"
+  printf '%s\n' ${pkgver##*.} > ${pkgbase}-${pkgver}/VERSION_PATCH
 }
 
 build() {
-    cmake -B build -S "${pkgbase}-${pkgver}" \
-        -G 'Unix Makefiles' \
-        -DCMAKE_BUILD_TYPE='None' \
-        -DCMAKE_INSTALL_PREFIX='/usr' \
-        -DSYSTEM_SPDLOG:BOOL='ON' \
-        -Wno-dev
-    cmake --build build
+  cmake -B build -S ${pkgbase}-${pkgver} \
+    -G 'Unix Makefiles' \
+    -DCMAKE_BUILD_TYPE=None \
+    -DCMAKE_INSTALL_PREFIX=/usr \
+    -DSYSTEM_SPDLOG:BOOL=ON \
+    -Wno-dev
+  cmake --build build
 }
 
 package_level-zero-headers() {
-    pkgdesc+=' (headers)'
+  pkgdesc+=" (headers)"
 
-    DESTDIR="$pkgdir" cmake --install build
-    install -D -m644 "${pkgbase}-${pkgver}/LICENSE" -t "${pkgdir}/usr/share/licenses/${pkgname}"
+  DESTDIR="${pkgdir}" cmake --install build
+  install -Dm644 ${pkgbase}-${pkgver}/LICENSE -t "${pkgdir}"/usr/share/licenses/${pkgname}
 
-    [ -d 'loader' ] && rm -rf loader
-    install -d loader/lib
-    mv "${pkgdir}/usr/lib"/*so* loader/lib
+  [ -d 'loader' ] && rm -rf loader
+  install -d loader/lib
+  mv "${pkgdir}"/usr/lib/*so* loader/lib
 }
 
 package_level-zero-loader() {
-    pkgdesc+=' (loader)'
-    depends=(
-        'gcc-libs'
-        'glibc'
-        'spdlog')
+  pkgdesc+=" (loader)"
+  depends=(
+    gcc-libs
+    glibc
+    spdlog)
 
-    mv loader "${pkgdir}/usr"
-    install -D -m644 "${pkgbase}-${pkgver}/LICENSE" -t "${pkgdir}/usr/share/licenses/${pkgname}"
+  mv loader "${pkgdir}"/usr
+  install -Dm644 ${pkgbase}-${pkgver}/LICENSE -t "${pkgdir}"/usr/share/licenses/${pkgname}
 }


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -1,12 +1,12 @@
 prepare() {
-printf '%s\n' "${pkgver##*.}" > "${pkgbase}-${pkgver}/VERSION_PATCH"
+printf '%s\n' ${pkgver##*.} > ${pkgbase}-${pkgver}/VERSION_PATCH
 }
 build() {
-cmake -B build -S "${pkgbase}-${pkgver}" \
+cmake -B build -S ${pkgbase}-${pkgver} \
 -G 'Unix Makefiles' \
--DCMAKE_BUILD_TYPE='None' \
--DCMAKE_INSTALL_PREFIX='/usr' \
--DSYSTEM_SPDLOG:BOOL='ON' \
+-DCMAKE_BUILD_TYPE=None \
+-DCMAKE_INSTALL_PREFIX=/usr \
+-DSYSTEM_SPDLOG:BOOL=ON \
 -Wno-dev
 cmake --build build
 }
