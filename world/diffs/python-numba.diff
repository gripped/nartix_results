--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -1,6 +1,6 @@
 
 pkgname=python-numba
-pkgver=0.58.0
+pkgver=0.57.1
 pkgrel=1
 pkgdesc="JIT compiler that translates a subset of Python and NumPy code into fast machine code"
 url="https://numba.pydata.org/"
@@ -12,13 +12,13 @@
   python
   python-llvmlite
   python-numpy
+  python-setuptools
 )
 makedepends=(
   onetbb
   openmp
   python-build
   python-installer
-  python-setuptools
   python-wheel
 )
 optdepends=(
@@ -46,13 +46,16 @@
   python-pyyaml
   python-scipy
 )
-source=(https://github.com/numba/numba/archive/$pkgver/$pkgname-$pkgver.tar.gz)
-sha256sums=('c8a12508d54553c7eb30f3563e9fe7f432ce541b31f9fa343da01b7e933e6bc5')
+source=(https://github.com/numba/numba/archive/$pkgver/$pkgname-$pkgver.tar.gz
+        numpy-1.25.patch)
+sha256sums=('3fa256ab0da922b9339711b9909201d02db41c3d31ba9b259d27b68eb3d3083c'
+            'fa8911a8df7b046658747c3a6542243f06fab70d08cfb882c8bc7de316da5d54')
 
 prepare() {
   cd numba-$pkgver
-  sed -e 's|1.26|1.27|' -i setup.py
-  sed -e 's|1, 25|1, 26|' -i numba/__init__.py
+  sed -e 's|1.25|1.26|' -i setup.py
+  sed -e 's|1, 24|1, 25|' -i numba/__init__.py
+  patch -p1 -i ../numpy-1.25.patch
 }
 
 build() {


############# Stripped diff below #################

--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -5,13 +5,13 @@
   python
   python-llvmlite
   python-numpy
+  python-setuptools
 )
 makedepends=(
   onetbb
   openmp
   python-build
   python-installer
-  python-setuptools
   python-wheel
 )
  via the CLI'
@@ -22,8 +22,9 @@
 )
 prepare() {
   cd numba-$pkgver
-  sed -e 's|1.26|1.27|' -i setup.py
-  sed -e 's|1, 25|1, 26|' -i numba/__init__.py
+  sed -e 's|1.25|1.26|' -i setup.py
+  sed -e 's|1, 24|1, 25|' -i numba/__init__.py
+  patch -p1 -i ../numpy-1.25.patch
 }
 build() {
   cd numba-$pkgver
