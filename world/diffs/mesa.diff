--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -39,7 +39,6 @@
   'rust'
   'spirv-llvm-translator'
   'spirv-tools'
-  'systemd'
   'vulkan-icd-loader'
   'wayland'
   'xcb-util-keysyms'
@@ -93,6 +92,8 @@
 
 build() {
   local meson_options=(
+    -D c_args=-I/usr/include,-I/usr/lib/clang/16/include
+    -D cpp_args=-I/usr/include,-I/usr/lib/clang/16/include
     -D android-libbacktrace=disabled
     -D b_ndebug=true
     -D dri3=enabled
@@ -125,11 +126,11 @@
     -D vulkan-drivers=amd,intel,intel_hasvk,swrast,virtio
     -D vulkan-layers=device-select,intel-nullhw,overlay
   )
-
   CFLAGS+=' -g1'
   CXXFLAGS+=' -g1'
-
-  arch-meson mesa-$pkgver build "${meson_options[@]}"
+  export CMAKE_PREFIX_PATH="/usr"
+  export CMAKE_INSTALL_LIBDIR="/usr"
+  artix-meson mesa-$pkgver build "${meson_options[@]}"
   meson configure build # Print config
   meson compile -C build
 
@@ -222,7 +223,6 @@
     'libdrm'
     'libx11'
     'libxshmfence'
-    'systemd'
     'wayland'
     'xcb-util-keysyms'
     'zstd'
@@ -244,7 +244,6 @@
     'libx11'
     'libxshmfence'
     'llvm-libs'
-    'systemd'
     'wayland'
     'xcb-util-keysyms'
     'zstd'
@@ -267,7 +266,6 @@
     'libx11'
     'libxshmfence'
     'llvm-libs'
-    'systemd'
     'wayland'
     'xcb-util-keysyms'
     'zstd'
@@ -289,7 +287,6 @@
     'libdrm'
     'libx11'
     'libxshmfence'
-    'systemd'
     'wayland'
     'xcb-util-keysyms'
     'zstd'


############# Stripped diff below #################

--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -31,7 +31,6 @@
   'rust'
   'spirv-llvm-translator'
   'spirv-tools'
-  'systemd'
   'vulkan-icd-loader'
   'wayland'
   'xcb-util-keysyms'
@@ -62,6 +61,8 @@
 }
 build() {
   local meson_options=(
+    -D c_args=-I/usr/include,-I/usr/lib/clang/16/include
+    -D cpp_args=-I/usr/include,-I/usr/lib/clang/16/include
     -D android-libbacktrace=disabled
     -D b_ndebug=true
     -D dri3=enabled
@@ -96,7 +97,9 @@
   )
   CFLAGS+=' -g1'
   CXXFLAGS+=' -g1'
-  arch-meson mesa-$pkgver build "${meson_options[@]}"
+  export CMAKE_PREFIX_PATH="/usr"
+  export CMAKE_INSTALL_LIBDIR="/usr"
+  artix-meson mesa-$pkgver build "${meson_options[@]}"
   meson configure build # Print config
   meson compile -C build
   DESTDIR="${srcdir}/fakeinstall" meson install -C build
@@ -167,7 +170,6 @@
     'libdrm'
     'libx11'
     'libxshmfence'
-    'systemd'
     'wayland'
     'xcb-util-keysyms'
     'zstd'
@@ -184,7 +186,6 @@
     'libx11'
     'libxshmfence'
     'llvm-libs'
-    'systemd'
     'wayland'
     'xcb-util-keysyms'
     'zstd'
@@ -202,7 +203,6 @@
     'libx11'
     'libxshmfence'
     'llvm-libs'
-    'systemd'
     'wayland'
     'xcb-util-keysyms'
     'zstd'
@@ -219,7 +219,6 @@
     'libdrm'
     'libx11'
     'libxshmfence'
-    'systemd'
     'wayland'
     'xcb-util-keysyms'
     'zstd'
