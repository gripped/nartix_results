--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -25,4 +25,6 @@
 
 package() {
   DESTDIR="$pkgdir" cmake --install build
+
+  rm -r $pkgdir/usr/lib/systemd
 }


############# Stripped diff below #################

--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -10,4 +10,5 @@
 }
 package() {
   DESTDIR="$pkgdir" cmake --install build
+  rm -r $pkgdir/usr/lib/systemd
 }
