--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -45,6 +45,7 @@
     --no-make \
     --qmake=/usr/bin/qmake6 \
     --api-dir /usr/share/qt6/qsci/api/python \
+    --scripts-dir=/usr/bin \
     --pep484-pyi
   cd build
   make


############# Stripped diff below #################

--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -14,6 +14,7 @@
     --no-make \
     --qmake=/usr/bin/qmake6 \
     --api-dir /usr/share/qt6/qsci/api/python \
+    --scripts-dir=/usr/bin \
     --pep484-pyi
   cd build
   make
