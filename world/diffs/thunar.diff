--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -40,5 +40,7 @@
 package() {
   cd $pkgname-$pkgver
   make DESTDIR="$pkgdir" install
+
+  rm -r $pkgdir/usr/lib/systemd
 }
 


############# Stripped diff below #################

--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -20,4 +20,5 @@
 package() {
   cd $pkgname-$pkgver
   make DESTDIR="$pkgdir" install
+  rm -r $pkgdir/usr/lib/systemd
 }
