--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -36,7 +36,7 @@
   local meson_options=(
   )
 
-  arch-meson pkgconf build "${meson_options[@]}"
+  artix-meson pkgconf build "${meson_options[@]}"
   meson compile -C build
 }
 


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -4,6 +4,6 @@
 build() {
 local meson_options=(
 )
-arch-meson pkgconf build "${meson_options[@]}"
+artix-meson pkgconf build "${meson_options[@]}"
 meson compile -C build
 }
