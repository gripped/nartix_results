--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -43,7 +43,7 @@
     -D legacy=enabled
   )
 
-  arch-meson libxml2 build "${meson_options[@]}"
+  artix-meson libxml2 build "${meson_options[@]}"
   meson compile -C build
 }
 


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -7,6 +7,6 @@
 -D icu=enabled
 -D legacy=enabled
 )
-arch-meson libxml2 build "${meson_options[@]}"
+artix-meson libxml2 build "${meson_options[@]}"
 meson compile -C build
 }
