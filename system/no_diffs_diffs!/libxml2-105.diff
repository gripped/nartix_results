--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -47,7 +47,7 @@
     -D legacy=enabled
   )
 
-  arch-meson libxml2 build "${meson_options[@]}"
+  artix-meson libxml2 build "${meson_options[@]}"
   meson compile -C build
 }
 


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -8,6 +8,6 @@
 -D icu=enabled
 -D legacy=enabled
 )
-arch-meson libxml2 build "${meson_options[@]}"
+artix-meson libxml2 build "${meson_options[@]}"
 meson compile -C build
 }
