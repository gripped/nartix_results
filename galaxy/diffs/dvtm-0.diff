--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -17,6 +17,7 @@
 
 	cp "${srcdir}/config.h" .
 	sed -i 's/CFLAGS =/CFLAGS +=/' config.mk
+	sed -i '/TERMINFO/Id' "Makefile"
 }
 
 build() {


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -2,6 +2,7 @@
 cd "${srcdir}/${pkgname}-${pkgver}"
 cp "${srcdir}/config.h" .
 sed -i 's/CFLAGS =/CFLAGS +=/' config.mk
+sed -i '/TERMINFO/Id' "Makefile"
 }
 build() {
 cd "${srcdir}/${pkgname}-${pkgver}"
