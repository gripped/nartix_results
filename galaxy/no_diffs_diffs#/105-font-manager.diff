--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -41,7 +41,7 @@
 		nautilus=true
 		reproducible=true
 	)
-	arch-meson "$_archive" build ${meson_opts[@]/#/-D }
+	artix-meson "$_archive" build ${meson_opts[@]/#/-D }
 	meson compile -C build
 }
 


############# Stripped diff below #################



--- Arch PKGBUILD
+++ Artix PKGBUILD
@@ -3,6 +3,6 @@
 nautilus=true
 reproducible=true
 )
-arch-meson "$_archive" build ${meson_opts[@]/#/-D }
+artix-meson "$_archive" build ${meson_opts[@]/#/-D }
 meson compile -C build
 }
